# Contributing

- Use Node 20+.
- Run `npm run build` and `npm run lint` before opening a PR.
- Keep new routes in `src/catalog/discord.routes.json` minimal and safe; set `confirm:true` for destructive ops.
